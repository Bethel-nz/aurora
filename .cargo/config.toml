[build]
# target-dir = "E:/aurora/target"  # Commented out for Linux/WSL compatibility

# Windows linker fix - use LLD to handle large Rust binaries (65k section limit)
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=-fuse-ld=lld"]